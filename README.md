# AVRS (Public Overview Site)

This repo hosts the canonical public AVRS overview page for GitHub Pages.

## Public exposure model (locked)

- Primary public artifact: the AVRS overview page (`index.html` + `assets/`)
- Domain binding: `avrs.aurenyx.com` (subdomain only)
- Not published here: white papers, module internals, Kotlin details beyond the one-liner, reactions/analyses, pitch language

## Deploy

GitHub Pages deploys via `.github/workflows/pages.yml`.

